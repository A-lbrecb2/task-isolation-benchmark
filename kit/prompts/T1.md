Refactor `TableListComponent` so the table rows are no longer hard-coded in the template.

Requirements:
1. Add an exported interface `Employee` with the fields `id: number`, `name: string`, `country: string`, `city: string`, `salary: number`.
2. Add a public property `employees: Employee[]` on the component containing exactly the six rows that are currently in the first table ("Simple Table"), with `salary` stored as a plain number (no currency symbol, no thousands separator).
3. Add a public method `totalSalary(): number` that returns the sum of all salaries.
4. Render both tables in the template with `*ngFor` over `employees`. Format the salary in the template as US dollars (for example `$36,738`).

Do not change any other component, module, or shared file. Keep the existing CSS classes.
