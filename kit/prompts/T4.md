Make the notification logic in `NotificationsComponent` testable.

Requirements:
1. Change `showNotification` to the signature `showNotification(from: string, align: string, message?: string): void`. When `message` is omitted, keep the current default text.
2. Add a public method `pickType(index: number): string` that maps an index 1 to 4 to `'info'`, `'success'`, `'warning'`, `'danger'` in that order and returns `'info'` for any other value. Use it inside `showNotification` instead of the inline array lookup.
3. Add a public property `lastNotification` of the shape `{ from: string; align: string; type: string; message: string } | null`, initially `null`, that is updated on every call of `showNotification`.
4. Keep calling `$.notify(...)` exactly as before, with `message` and `type` taken from the values above.

Do not change any other component, module, or shared file.
